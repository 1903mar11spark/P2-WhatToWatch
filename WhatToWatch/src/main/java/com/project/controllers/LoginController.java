package com.project.controllers;

import org.springframework.stereotype.Controller;

@Controller
public class LoginController {
	
	/*
	 * @PostMapping(value="/login",
	 * consumes=MediaType.APPLICATION_FORM_URLENCODED_VALUE) public RedirectView
	 * handleFormRequest(@RequestBody MultiValueMap<String, String> formParams,
	 * RedirectAttributes attributes) { attributes.addFlashAttribute("username",
	 * formParams.getFirst("username")); // could grab password, do some
	 * authentication return new RedirectView("profile"); }
	 */

}
